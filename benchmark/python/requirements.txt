# Benchmark orchestration + analysis dependencies. Maintainer-only — never
# bundled into the published Dart package.
#
# Install:  pip install -r benchmark/python/requirements.txt
# Python:   3.10+

# Numerical + statistical analysis.
numpy>=1.26,<3.0
scipy>=1.11,<2.0

# DataFrames for JSON wrangling.
pandas>=2.1,<3.0

# Charts. matplotlib for static PNG/SVG (PR-friendly), plotly optional for
# interactive HTML exploration.
matplotlib>=3.8,<4.0

# HTML report templating.
jinja2>=3.1,<4.0
